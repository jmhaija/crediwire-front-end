/**
 * List of browser minimum requirements.
 * 
 * If browser flag is listed under the name property, then that browser must
 * meet the version number listed under the version property.
 * 
 * Any browser not listed is supported; i.e. this is a blacklist.
 */
define([], [
    { name : 'msie', version : 11 }
]);
